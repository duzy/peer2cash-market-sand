project (tools/gcc/c++)

CXXFLAGS += $(@.variant_cflags)
CXXFLAGS += -DLEVELDB_PLATFORM_POSIX
CXXFLAGS += -I"$(dirdir $/)" -I"$(dirdir $/)/include"

objects := memenv.o

libmemenv.a: lib.a($@, $(objects))

clean:[(stderr) (stdout) (shell)]
	@echo "clean: $." && rm -vf *.a *.o

use:
	CXXFLAGS += -I"$/"
	LDFLAGS += -L"$/"
	LDLIBS += -lmemenv
