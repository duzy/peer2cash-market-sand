project

import (
    './src'
)

libs = $(src.xlibs)

build: src.archives src.objects src.programs
clean: src.clean

use:
#	compile_flags += -I"$//src"
#	link_flags += -L"$//src"
#	libs += "$//src/bitcoind-bitcoind.o"
#	libs += $(libs)
